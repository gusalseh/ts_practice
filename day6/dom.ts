let physicalDOM = document.createElement("p");
physicalDOM.innerText = 'Hello physicalDOM world!';

document.body.appendChild(physicalDOM);

let spanPhysicalDOM = document.createElement("span");
spanPhysicalDOM.innerText = "span test"

document.body.appendChild(spanPhysicalDOM);